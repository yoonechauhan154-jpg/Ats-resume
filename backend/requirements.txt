# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Parsing
pdfplumber>=0.11
PyMuPDF>=1.24
python-docx>=1.1
pillow>=10.2
pytesseract>=0.3.10

# NLP / scoring
# NOTE: spacy and sentence-transformers are optional at runtime — the app
# degrades gracefully (TF-IDF fallback) if they are not installed.
spacy>=3.7
scikit-learn>=1.4

# AI client (OpenAI-compatible SDK for TokenRouter + Ollama)
openai>=1.30

# Rate limiting (Upstash Redis)
redis>=5.0
upstash-redis>=0.19

# HTTP client used for tests
httpx>=0.27
